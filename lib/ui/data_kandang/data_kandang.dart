import 'package:flutter/material.dart';

class DataKandang extends StatelessWidget {
  const DataKandang({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
