import 'package:flutter/material.dart';

class DataAyam extends StatelessWidget {
  const DataAyam({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
