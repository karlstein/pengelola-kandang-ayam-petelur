import 'package:flutter/material.dart';

class DataPenempatan extends StatelessWidget {
  const DataPenempatan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
