import 'package:flutter/material.dart';

class DataProduksi extends StatelessWidget {
  const DataProduksi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
