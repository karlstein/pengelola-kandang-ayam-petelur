import 'package:flutter/material.dart';

class ProduksiBulanan extends StatelessWidget {
  const ProduksiBulanan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
